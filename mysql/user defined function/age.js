
function userDefinedFunction(name){
    if(age == undefined)
        throw "age not defined";

    if (age < 50)
        return ('under 50');
    else if (age > 100)
        return ('above 50');
    else 
        return 'undefined';            

}